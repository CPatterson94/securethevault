// create variables that will output the correct comibnation.

const combo1 = 5 * 2;
const combo2 = 80 / 2;
const combo3 = 40 - 1;

// Create a popup window explaining you have gained access to the vault.

alert(
  "You have received this message because you have been chosen to open an important vault."
);
// Another popup letting you know what the combination is and it outputs the variables and list them out.
alert(
  "Here is the secret combination:" + " " + combo1 + " " + combo2 + " " + combo3
);
